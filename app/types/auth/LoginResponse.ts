export type LoginResponse = {
  _id: string;
  token: string;
  name: string;
  email: string;
  phone: string;
  favourites: [];
};

export type User = {
  _id: string;
  token: string;
  name: string;
  email: string;
  phone: string;
  favourites: [];
};
